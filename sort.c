#include <stdio.h>
#include "main.h"
#include "utils.h"

extern int UNSORTED_ARRAY[];

static void bubbleSort(int unsortedArray[], int sortedArray[], int length)
{
}

int main ()
{
    int sortedArray[NUM_ELTS];
    printArray(UNSORTED_ARRAY, NUM_ELTS);

    return 0;
}
